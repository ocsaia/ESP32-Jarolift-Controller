## OS specific ########
.DS_Store

## Project files ######
.pio
.clang_complete
.gcc-flags.json
.DS_Store
.vscode/settings.json
.vscode/launch.json
.vscode/extensions.json
.vscode/c_cpp_properties.json
.travis.yml

connect.txt
zTest/
TODO.txt

## Device backups - never commit these #######################################
# A config.json read off a device carries the KeeLoq master keys, the base
# serial every channel's device key is derived from, and the WiFi and MQTT
# credentials. This fork is public. Anchored to the root on purpose: the
# tracked web/output/config.json is the simulator's dummy file and must stay
# tracked.
#
# Every root-level .json, not just the one obvious name: the first version of
# this rule matched /config.json alone, and the very next file to arrive was
# config_after-upgrade.json, which slipped straight past it. The repository
# tracks no .json at its root, so there is nothing to lose by covering all of
# them - and a config pulled off a device rarely keeps its original name.
/*.json

# Full or partial flash dumps taken with esptool before flashing. Same secrets,
# plus the NVS rolling counter.
/backup-*.bin
